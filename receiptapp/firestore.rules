rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Receipts are only accessible to authenticated (bootstrapped) users.
    match /receipts/{receiptId} {
      allow read, write: if request.auth != null;
    }
  }
}
